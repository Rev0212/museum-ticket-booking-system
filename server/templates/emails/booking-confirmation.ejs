<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { text-align: center; padding: 20px 0; }
    .ticket-info { margin: 20px 0; }
    .ticket-item { margin-bottom: 5px; }
    .total { margin-top: 20px; font-weight: bold; }
    .footer { margin-top: 30px; font-size: 12px; color: #777; text-align: center; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Booking Confirmation</h1>
      <p>Thank you for booking with Indian Heritage Museums!</p>
    </div>
    
    <p>Dear <%= userName %>,</p>
    
    <p>Your booking has been confirmed with the following details:</p>
    
    <div class="ticket-info">
      <p><strong>Booking Reference:</strong> <%= bookingReference %></p>
      <p><strong>Museum:</strong> <%= museumName %></p>
      <p><strong>Visit Date:</strong> <%= formattedDate %></p>
      
      <h3>Tickets:</h3>
      <% tickets.forEach(function(ticket) { %>
        <div class="ticket-item">
          <p><%= ticket.quantity %> x <%= ticket.type %> (₹<%= ticket.price %> each)</p>
        </div>
      <% }); %>
      
      <div class="total">
        <p>Total Amount: ₹<%= totalAmount %></p>
      </div>
    </div>
    
    <p>Please show the attached QR code at the entrance for easy check-in.</p>
    
    <p>We look forward to your visit!</p>
    
    <div class="footer">
      <p>© <%= new Date().getFullYear() %> Indian Heritage Museums. All rights reserved.</p>
    </div>
  </div>
</body>
</html>